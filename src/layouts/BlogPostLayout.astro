---
// 1. The frontmatter prop gives access to frontmatter and other data
const { frontmatter } = Astro.props;
---
<!DOCTYPE html>
<html lang="en" class="min-h-screen bg-gray-50">
  <head>
    <!-- Add other Head elements here, like styles and meta tags. -->
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta charset="utf-8">
    <title>{frontmatter.title}</title>
  </head>
  <body class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <div class="bg-white shadow-md rounded-lg overflow-hidden p-6 space-y-4">
      <header class="border-b pb-4 mb-6">
        <h1 class="text-3xl font-extrabold text-gray-900 tracking-tight">
          {frontmatter.title} 
          <span class="text-lg text-gray-600 font-normal block mt-2">
            by {frontmatter.author}
          </span>
        </h1>
      </header>

      <!-- 2. Rendered HTML will be passed into the default slot. -->
      <article class="prose lg:prose-xl prose-headings:text-gray-900 prose-a:text-blue-600 prose-a:underline">
        <slot />
      </article>

      <footer class="text-sm text-gray-500 italic border-t pt-4">
        Written on: {frontmatter.date}
      </footer>
    </div>
  </body>
</html>